/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "img.clerk.com",
            "www.gutenberg.org"
        ]
    }
};

export default nextConfig;
